import Link from'next/link';import{notFound}from'next/navigation';import{products}from'../data';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=products.find(x=>x.slug===slug);return p?{title:`${p.name} | 작품샵`,description:p.description,openGraph:{images:[]},twitter:{images:[]}}:{title:'작품을 찾을 수 없습니다'}}
export default async function ProductDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <section className="page-content product-detail"><div className="product-detail-image"><img src="/images/hero.png" alt={`${p.category} 작품 이미지 준비 중`}/></div><div><p className="eyebrow">{p.category}</p><h1>{p.name}</h1><p className="body">{p.description}</p><dl><dt>가격</dt><dd>{p.price}</dd><dt>판매상태</dt><dd>{p.status}</dd></dl><p className="notice">실제 작품 정보와 이미지는 준비되는 대로 업데이트됩니다.</p><Link className="button" href={`/contact?type=product&product=${p.slug}`}>구매 문의하기</Link></div></section>}
